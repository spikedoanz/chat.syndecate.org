[
  {zulip_signal, [
    %% Zulip API credentials
    {zulip_url, "https://chat.syndecate.org"},
    {zulip_email, "CHANGEME-bot@chat.syndecate.org"},
    {zulip_api_key, "CHANGEME"},

    %% Signal configuration
    {signal_cli_path, "signal-cli"},
    {signal_account, "+1CHANGEME"},
    {signal_group_id, "CHANGEME_BASE64_GROUP_ID"},

    %% Channel filter: list of stream IDs to forward.
    %% Empty list = forward all channels.
    {channel_filter, []}
  ]}
].
